import { ForbiddenSymbolValidatorDirective } from './forbidden-symbol-validator.directive';

describe('ForbiddenSymbolValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenSymbolValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
