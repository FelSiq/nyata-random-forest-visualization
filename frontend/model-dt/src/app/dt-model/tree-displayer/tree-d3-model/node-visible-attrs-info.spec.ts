import { NodeVisibleAttrsInfo } from './node-visible-attrs-info';

describe('NodeVisibleAttrsInfo', () => {
  it('should create an instance', () => {
    expect(new NodeVisibleAttrsInfo()).toBeTruthy();
  });
});
